void operatorControl() 
{
while (true)
{
if (digitalRead(11) == 0)
{
	motorSet(1, 127);
	motorSet(10,127);
	}

	if (digitalRead(11) == 1)
{
		motorSet(1, 0);
		motorSet(10,0);
	}
}
}

This test code would let us see if spinning the hook at max speed would actually get the star over the fence.
